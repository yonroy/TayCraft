# K1 · Nền tảng AI

Phiếu khóa **K1** (free cho mọi người — lead magnet). Gồm các PHẦN:
- **A — Toán nền tảng** (vectơ, ma trận, đạo hàm, xác suất)
- **B — Học máy cổ điển** (hồi quy, k-NN, k-means, cây quyết định…)
- **C — Nơ-ron & MLP** (lớp tuyến tính → activation → nơ-ron → MLP → softmax)

Asset dùng chung (`wb.css`, `wb-random.js`, `index.html`) ở thư mục cha → phiếu link bằng `../`.
Tạo phiếu mới: `node ../tools/new.mjs <mã> <slug> "<Tựa>" "<English>"` (tự đặt vào đúng khóa).
